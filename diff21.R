#given an input value, n, return the absolute difference between n and 21. 
#return double the absolute differene if n is > 21

differ <- function(n){
      if(n <= 21){
         return(abs(21-x))   
      } else if(x > 21){
            return(2*abs(21-x))
      }
}